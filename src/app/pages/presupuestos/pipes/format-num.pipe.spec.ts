import { FormatNumPipe } from './format-num.pipe';

describe('FormatNumPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumPipe();
    expect(pipe).toBeTruthy();
  });
});
